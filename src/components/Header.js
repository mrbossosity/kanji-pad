import styled from "styled-components";

const HeaderContainer = styled.div`
    font-size: 2rem;
    color: #ddd;
`;

function Header() {
    return <HeaderContainer>Kanji Pad</HeaderContainer>;
}

export default Header;
